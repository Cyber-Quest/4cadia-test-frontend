import { makeStyles } from "@material-ui/core/styles";

export const styles = makeStyles((theme) => ({
  textField: {
    width: "100%"
  },
  formControl: { 
    marginTop: 16,
    minWidth: "100%",
  },
}));
